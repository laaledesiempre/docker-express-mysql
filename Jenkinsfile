pipeline {
    agent any
    stages {
        stage("Build images") {
            steps {
                sh 'echo todo Building images'
            }
        }

        stage("Push images to Dockerhub") {
            steps {
                sh 'echo todo Pushing images to dockerhub'
            }
        }
        
        stage("Advise to update") {
            steps {
                sh 'echo  todo Is time to update the kctl'
            }
        }
    }
}